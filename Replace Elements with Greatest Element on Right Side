public int[] ReplaceElements(int[] arr) {
        int right=-1,current=0;
        for(int i=arr.Length-1;i>=0;i--){
            current=arr[i];
            arr[i]=right;
            if(current>right){
                right=current;
            }
        }
        return arr;
}
